import styles from '@/styles/Home.module.css';

const Gallery = () => {
  return (
    <>
      <main
        className={`${styles.main} bg-gradient-to-r from-slate-300 to-violet-400`}
      ></main>
    </>
  );
};

export default Gallery;
